#!/bin/bash

for file in ~/github/*
do
    if [ -d $file ] ; then
        echo $file
        cd $file;
        git status;
        git add .;
        git commit -m "commit $(date)"
        git pull;
        git push;
    fi
done
